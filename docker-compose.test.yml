services:
  test:
    image: golang:1.26-alpine
    working_dir: /src
    volumes:
      - .:/src
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/root/.cache/go-build
    environment:
      CGO_ENABLED: "1"
    command: sh -c "apk add --no-cache git build-base >/dev/null && go vet ./... && go test -race -count=1 ./..."

volumes:
  go-mod-cache:
  go-build-cache:
